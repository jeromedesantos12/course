function selamatPagi(nama) {
  // nama = parameter
  console.log(`Halo, ${nama}. Selamat Pagi!`);
}

// versi arrow function
// const selamatPagi = (nama) => console.log(`Halo, ${nama}. Selamat Pagi!`);

selamatPagi(`Aim`); // `Aim` = argument
